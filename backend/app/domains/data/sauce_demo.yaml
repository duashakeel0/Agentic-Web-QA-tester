name: sauce_demo
base_url: https://www.saucedemo.com
workflows:
  - name: login
    steps:
      - Navigate to saucedemo.com
      - Enter "standard_user" into the Username field
      - Enter "secret_sauce" into the Password field
      - Click the Login button
    expected_outcome:
      url_contains: /inventory.html
      text_contains: Products

  - name: add_to_cart
    steps:
      - Navigate to saucedemo.com
      - Enter "standard_user" into the Username field
      - Enter "secret_sauce" into the Password field
      - Click the Login button
      - Click "Add to cart" on a product
      - Open the cart
    expected_outcome:
      url_contains: /cart.html

  - name: checkout
    steps:
      - Navigate to saucedemo.com
      - Enter "standard_user" into the Username field
      - Enter "secret_sauce" into the Password field
      - Click the Login button
      - Click "Add to cart" on a product
      - Open the cart
      - Go to checkout and fill in the shipping information
      - Complete the order
    expected_outcome:
      url_contains: /checkout-complete.html
      text_contains: Thank you for your order
